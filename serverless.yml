service: makeupiv
frameworkVersion: '2'

provider:
  name: aws
  runtime: ruby2.7
  stage: dev
  region: eu-west-3
  memorySize: 128
  environment:
    TELEGRAM_TOKEN: ${env:TELEGRAM_TOKEN}

# you can add statements to the Lambda function's IAM Role here
#  iamRoleStatements:
#    - Effect: "Allow"
#      Action:
#        - "s3:ListBucket"
#      Resource: { "Fn::Join" : ["", ["arn:aws:s3:::", { "Ref" : "ServerlessDeploymentBucket" } ] ]  }
#    - Effect: "Allow"
#      Action:
#        - "s3:PutObject"
#      Resource:
#        Fn::Join:
#          - ""
#          - - "arn:aws:s3:::"
#            - "Ref" : "ServerlessDeploymentBucket"
#            - "/*"

# you can define service wide environment variables here
#  environment:
#    variable1: value1

#    - exclude-me-dir/**

functions:
  datospersonales:
    handler: handler.datospersonales
    events:
      - http:
          path: /
          method: post
    environment:
      TG_TOKEN: ${ssm:makeup-token~true}
